apiVersion: v1
kind: Service
metadata:
  name: fluxora-redis
  namespace: fluxora
  labels:
    app: fluxora-redis
    component: cache
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    app: fluxora-redis
