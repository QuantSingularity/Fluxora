apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fluxora-redis-pvc
  namespace: fluxora
  labels:
    app: fluxora-redis
    component: cache
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
