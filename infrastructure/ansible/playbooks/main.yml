---
- name: Configure web servers
  hosts: webservers
  become: true
  become_method: sudo
  gather_facts: true
  roles:
    - common
    - webserver

- name: Configure database servers
  hosts: databases
  become: true
  become_method: sudo
  gather_facts: true
  roles:
    - common
    - database
